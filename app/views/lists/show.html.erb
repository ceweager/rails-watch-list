<div class="container">
  <h2><%= @list.name %></h2>
  <%= link_to 'create bookmark', new_list_bookmark_path(@list) %>
  <div class="row">
    <% @movies.each do |movie| %>
        <div class="card">
        <h1><%= movie.title %></h1>
        </div>
    <% end %>
  </div>
</div>
